int... countdown(int counter) {
  while (counter > 0) {
    yield counter;
    --counter;
  }
}
assert(type.of(countdown) == "int...(int)");
for (var i : countdown(5)) {
  assert(type.of(i) == "int");
  print(i);
}
///<RUNTIME><ERROR><RESOURCE>(1,8): Generators are not yet supported
