int... countdown(int counter) {
  while (counter > 0) {
    yield counter;
    --counter;
  }
}
assert(type.of(countdown) == "int...(int)");
var f = countdown(5);
assert(type.of(f) == "int...");
while (var i = f()) {
  assert(type.of(i) == "int");
  print(i);
}
///OLD>5
///OLD>4
///OLD>3
///OLD>2
///OLD>1
///NEW<RUNTIME><ERROR><RESOURCE>(1,8): Generators are not yet supported
