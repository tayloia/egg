type Holder {
  static int a = 1;
  static int b() { return 2; }
  int c;
  int d();
};
assert(Holder.c == 1);
///<COMPILER><ERROR><RESOURCE>(7,8-15): Type 'Holder' does not support the static member 'c' (though values of type 'Holder' support a property with that name)
